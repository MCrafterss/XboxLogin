<?php

namespace Andre\XboxOnly\Decode;

use Andre\XboxOnly;
use pocketmine\scheduler\AsyncTask;
use pocketmine\utils\Utils;
use pocketmine\Server;

 private $string = null;
 
class StringDecode extends PluginTask{
	
	     public function __construct(Plugin $owner){
          parent::__construct($owner);
          $this->plugin = $owner;
		 }
}
}

    /*
	 Ignore this, ALLLOT still needs to be done! ^_^
	  Don't worry, it"ll be finishe in no time!
	*/